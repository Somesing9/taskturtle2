<div class="row">
  <div class="col-md-6">
    <h3>{{job.title}}</h3>
    <p>{{job.description}}</p>
    <p>Bids</p>
    <ul>
      {{#each bids}}
        <li>{{amount}}
      {{/each}}
    </ul>
  </div>
</div>